<% provide(:title, 'Edit Team') %>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <h4>Editing <%= "#{@team.name}" %></h4>
    
    <%= render 'form' %>
    
    <%= link_to "Cancel", team_path(@team), class: "btn btn-outline-dark" %>
    
  </div>
</div>